#include <iostream>
using namespace std;

int main() {
	int n, money;
	int cost[10] = { 0, };
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		scanf("%d", &cost[i]);
	scanf("%d", &money);

	return 0;
}